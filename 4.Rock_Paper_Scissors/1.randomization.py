# Random Module.

import random   # importing module to use functions.
print(random.randint(1, 10))    # random.randint(start, stop)  -----> returns an integer in the specified range (both inclusive).
print(random.random())   # random.random(start, stop) ---> returns a floating point number between the specified range (but not stop).
