# random.choice()

## It is used to pick a random item from a list.
import random

name_list = ['tej', 'akhil']
print(random.choice(name_list))  # passing a list as an argument to choice function.