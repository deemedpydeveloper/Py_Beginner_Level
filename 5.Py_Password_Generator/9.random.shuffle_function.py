# Shuffling items in a list.

## random.shuffle(list) ---> randomizes the items ina list.
import random

num_list = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]
random.shuffle(num_list)
print(num_list)  # every time prints out random list.