# Rounding a number.

# round() ---> instead of long float decimal values after decimal point, we can round it to user requirement using this function.
result = 8 / 3
print(result)
print(round(result, 2))  # rounding-off to two decimal places.